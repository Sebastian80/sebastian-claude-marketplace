# project-memory-integration

Claude Code plugin for project-scoped memory persistence.

## Commands

- `/pm:save` - Save current session state
- `/pm:load` - Load project memories
- `/pm:search` - Search across memories

## Requires

- `pm` CLI installed: `pip install git+https://github.com/Sebastian80/project-memory.git`
